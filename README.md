# GPT-1-Clone
A custom Hindi-focused language model inspired by GPT architecture. Built the backend, trained from scratch on a Hindi corpus, expanded with tuned hyperparameters and experimented with alternative attention mechanisms to improve performance and language understanding.
